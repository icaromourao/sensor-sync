import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  chartsContainer: {},
  chartTitle: {
    fontSize: 16,
    color: '#1B1B1B',
  },
  chart: {
    height: 200,
    width: '100%',
    marginBottom: 32,
  },
});

export default styles;
